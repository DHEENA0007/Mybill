name: billpro_mobile
description: "BillPro Admin - Billing & Inventory Management"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  # Networking
  dio: ^5.7.0
  # State Management
  provider: ^6.1.2
  # Local Storage
  shared_preferences: ^2.3.4
  # UI Helpers
  intl: ^0.20.2
  fl_chart: ^0.70.2
  shimmer: ^3.0.0
  google_fonts: ^6.2.1
  flutter_staggered_animations: ^1.1.1
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
